package com.madteam.sunset.ui.theme

import androidx.compose.ui.text.TextStyle
import androidx.compose.ui.text.font.Font
import androidx.compose.ui.text.font.FontFamily
import androidx.compose.ui.unit.sp
import com.madteam.sunset.R

val primaryBoldDisplayM = TextStyle(
    fontFamily = FontFamily(Font(R.font.montserrat_bold)),
    fontSize = 39.sp
)

val primaryBoldDisplayS = TextStyle(
    fontFamily = FontFamily(Font(R.font.montserrat_bold)),
    fontSize = 33.sp
)

val primaryBoldHeadlineL = TextStyle(
    fontFamily = FontFamily(Font(R.font.montserrat_bold)),
    fontSize = 27.sp
)

val primaryBoldHeadlineM = TextStyle(
    fontFamily = FontFamily(Font(R.font.montserrat_bold)),
    fontSize = 23.sp
)

val primaryBoldHeadlineS = TextStyle(
    fontFamily = FontFamily(Font(R.font.montserrat_bold)),
    fontSize = 19.sp
)

val primaryMediumDisplayM = TextStyle(
    fontFamily = FontFamily(Font(R.font.montserrat_medium)),
    fontSize = 39.sp
)

val primaryMediumDisplayS = TextStyle(
    fontFamily = FontFamily(Font(R.font.montserrat_medium)),
    fontSize = 33.sp
)

val primaryMediumHeadlineL = TextStyle(
    fontFamily = FontFamily(Font(R.font.montserrat_medium)),
    fontSize = 27.sp
)

val primaryMediumHeadlineS = TextStyle(
    fontFamily = FontFamily(Font(R.font.montserrat_medium)),
    fontSize = 19.sp
)

val primaryMediumHeadlineXS = TextStyle(
    fontFamily = FontFamily(Font(R.font.montserrat_medium)),
    fontSize = 16.sp
)

val secondaryRegularBodyL = TextStyle(
    fontFamily = FontFamily(Font(R.font.montserrat_regular)),
    fontSize = 16.sp
)

val secondarySemiBoldHeadLineS = TextStyle(
    fontFamily = FontFamily(Font(R.font.montserrat_semibold)),
    fontSize = 19.sp
)

val secondarySemiBoldHeadLineM = TextStyle(
    fontFamily = FontFamily(Font(R.font.montserrat_semibold)),
    fontSize = 23.sp
)

val secondarySemiBoldBodyM = TextStyle(
    fontFamily = FontFamily(Font(R.font.montserrat_semibold)),
    fontSize = 13.sp
)

val secondarySemiBoldBodyS = TextStyle(
    fontFamily = FontFamily(Font(R.font.montserrat_semibold)),
    fontSize = 11.sp
)

val secondaryRegularHeadlineM = TextStyle(
    fontFamily = FontFamily(Font(R.font.montserrat_regular)),
    fontSize = 23.sp
)

val secondaryRegularHeadlineS = TextStyle(
    fontFamily = FontFamily(Font(R.font.montserrat_regular)),
    fontSize = 19.sp
)

val secondaryRegularBodyM = TextStyle(
    fontFamily = FontFamily(Font(R.font.montserrat_regular)),
    fontSize = 13.sp
)

val secondaryRegularBodyS = TextStyle(
    fontFamily = FontFamily(Font(R.font.montserrat_regular)),
    fontSize = 11.sp
)